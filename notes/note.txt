@Autowire vs Constructor Injection (constructor injections is preferred)
    favours immutability & forces object creation with the injected object & no reflection & support multithreading

Log levels
    trace > debug > info > warn > error

for set up intellij global config variable execute below command
    export TWITTER_BEARER_TOKEN = YOUR_BEARER_TOKEN

'oauth_access_token' => Access token
'oauth_access_token_secret' => Access token secret
'consumer_key' => API key
'consumer_secret' => API secret

we use separate config repository as a 12 factor architecture methodology
    https://12factor.net/

springDoc openAPI (swagger V3) supports content negotiation versioning but swagger V2 doesn't support that feature

swagger annotations
    @Operation, @ApiResponse, @Content, @Schema

 when we using spring cloud loadbalancer we need to setup instances manually(services.yml file)
 each instance will get call round robin pattern(default way) from this load balancing mechanism